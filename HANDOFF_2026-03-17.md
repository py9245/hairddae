# Handoff 2026-03-17

## Session

- Conversation fork id: `019cf6cb-de38-7cc2-9527-3f3992cc884b`
- Note: this id itself cannot be stored/restored by Git. It is recorded here only for reference.

## Branch

- Current branch: `feat/hair-recommend-ws`

## What Changed

- Added direct inference flow for camera preview:
  - FE `hairapplybootstrap` / `hairapplyresume`
  - direct WSS to inference
  - FE overlay from `render_task`
- Added Python `Inference/` service and compose integration
- Added on-screen perf HUD on camera page:
  - `draw fps`
  - `rtt ms`
  - `proc fps`
  - `bundle ms`
- Fixed local WSS 403 issue caused by empty `INFERENCE_JWT_SECRET`
- Fixed overlay invisibility caused by cropped `hair_rgba` PNG coordinate mismatch

## Current Important Files

- FE session / overlay:
  - `FE/src/hooks/Camera/useHairInferenceSession.ts`
  - `FE/src/hooks/Camera/useHairOverlayCanvas.ts`
  - `FE/src/lib/Camera/inference.ts`
  - `FE/src/lib/Camera/overlay.ts`
  - `FE/src/components/Camera/FaceLandmarksView.tsx`
- Inference:
  - `Inference/app/main.py`
  - `Inference/app/catalog.py`
  - `Inference/app/render.py`
  - `Inference/app/config.py`
- BE / infra:
  - `BE/src/main/java/com/example/beapp/api/HomeController.java`
  - `BE/src/main/java/com/example/beapp/service/HomeService.java`
  - `BE/docker-compose.local.yml`
  - `BE/docker-compose.yml`
  - `BE/nginx/local.conf`
  - `BE/nginx/default.conf`

## Current State

- Camera overlay is visible again.
- Recent tuning made the system feel slower than `/home/yusin/hairddae`.
- Main reason:
  - current stack is `browser tracking -> WSS -> inference asset select -> browser asset fetch -> browser draw`
  - `hairddae` is effectively single-process local compose/render with in-memory asset caching
- Recent hysteresis tuning was increased to reduce asset thrash:
  - `INFERENCE_HYSTERESIS_MARGIN=4.0`
  - `INFERENCE_MIN_HOLD_MS=400`
  - This improves stability but hurts responsiveness.

## Latest Local Verification

- FE build: passed
- Inference tests: passed (`4 passed`)
- Inference local container: rebuilt
- Local nginx served latest FE bundle after sync to `BE/nginx/html`

## Useful Commands Tomorrow

- Check current branch:
  - `git branch --show-current`
- See working tree:
  - `git status --short`
- FE build:
  - `corepack pnpm --dir FE build`
- Inference tests:
  - `cd Inference && uv run pytest`
- Rebuild local inference:
  - `docker compose -f BE/docker-compose.local.yml up -d --build inference`
- Sync FE dist to local nginx html:
  - `rsync -a --delete FE/dist/ BE/nginx/html/`

## Recommended Next Step

- If speed is the top priority, first retune:
  - `INFERENCE_HYSTERESIS_MARGIN`
  - `INFERENCE_MIN_HOLD_MS`
- Then compare on camera HUD:
  - `draw fps`
  - `rtt ms`
  - `proc fps`
  - `bundle ms`

