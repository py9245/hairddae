Archived scripts with no in-repo references as of 2026-03-24.

These files were moved out of the active tool set to reduce clutter in
`hairddae_tools/` without deleting potentially useful one-off scripts.

Restore a script to `hairddae_tools/` if it becomes part of an active
workflow again.
