[project]
name = "hairapply-inference"
version = "0.1.0"
description = "Feature-only websocket inference service for hair apply"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115,<1.0",
  "PyJWT>=2.10,<3.0",
  "redis>=5.2,<6.0",
  "uvicorn[standard]>=0.34,<1.0",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1.0",
  "pytest>=8.3,<9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
