# 2026-03-10 S14P21M101-562

## 이슈
- [W2][BE] 내 정보 조회 API(GET /me)
- Jira: `S14P21M101-562`

## 작업 요약
- 신규 엔드포인트 `/api/me`를 추가했다.
- 응답 DTO `MeResponse`를 도입해 `userID`, `age`, `gender`를 함께 반환하도록 구성했다.
- `MypageService`에서 인증 사용자 정보를 실제로 조회해 내 정보 응답을 만들도록 보강했다.
- Security 설정에 `/api/me`를 인증 필요 경로로 추가했다.
- 인증 통합 테스트를 `/api/me` 기준으로 옮겨 무토큰 `401`, 로그인 후 `200`과 사용자 기본 정보 응답을 검증했다.

## 변경 파일
- `BE/src/main/java/com/example/beapp/api/MeController.java`
- `BE/src/main/java/com/example/beapp/api/dto/mypage/MeResponse.java`
- `BE/src/main/java/com/example/beapp/service/MypageService.java`
- `BE/src/main/java/com/example/beapp/config/SecurityConfig.java`
- `BE/src/test/java/com/example/beapp/api/ApiSecurityIntegrationTest.java`

## 검증
- `docker compose -f BE/docker-compose.local.yml up -d --build postgres redis app nginx`
- 무토큰으로 `/api/me` 호출 시 `401` 확인
- 로그인 후 `/api/me` 호출 시 `200`과 `userID=TestUser01`, `age=25`, `gender=M` 확인
- `docker compose -f BE/docker-compose.local.yml down`

## 비고
- 기존 `/api/mypage/user`는 유지하고, 신규 `/api/me`를 추가해 과제 요구 경로를 충족했다.
- 2026-03-10 기준 `BE` 브랜치에는 `merge: S14P21M101-562 into BE` 커밋으로 반영됐다.
